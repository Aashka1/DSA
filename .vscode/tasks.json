{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "C/C++: g++.exe build active file",
        "type": "shell",
        "command": "g++",
        "args": [
          "-g",                       // Enable debugging information
          "${file}",                  // Current file (e.g., .cpp file)
          "-o",                       // Output flag
          "${fileDirname}\\${fileBasenameNoExtension}.exe"  // Output executable name
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": ["$gcc"],
        "detail": "Generated task for building the active C++ file"
      }
    ]
  }
  